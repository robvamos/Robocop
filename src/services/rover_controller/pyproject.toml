[project]
name = "robocop-rover-controller"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["../../../tests/rover_controller"]
