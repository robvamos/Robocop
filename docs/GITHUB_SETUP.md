# GitHub Setup

## Current Local Project

Local path:

```text
C:\node-v24.15.0-win-x64\CodexRoot\robocop
```

## Intended Repository

Suggested GitHub repository:

```text
robvamos/robocop
```

## Requirements

- Git must be installed or available in `PATH`, or
- the GitHub connector must be re-authorized for this Codex session.

## Setup With Git

From this folder:

```powershell
git init
git branch -M main
git add .
git commit -m "Initial Robocop project structure"
git remote add origin https://github.com/robvamos/robocop.git
git push -u origin main
```

